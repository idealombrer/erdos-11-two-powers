# Worklog — research record (#11 two-powers variant)

> **⚠️ Research worklog. Includes dead ends and corrected errors — kept for transparency.**
>
> These are chronological working notes across ~56 sessions, not a polished document. They
> contain hypotheses later refuted, quantitative framings later corrected (e.g. which moment is
> "binding", whether a moments improvement is unconditional, an over-claimed García–Voloch/
> Bourgain–Chang application caught before it reached the paper), and pistes that were closed
> with the exact blocking inequality. They are retained on purpose: the corrected mistakes show
> that errors were caught (by the validator role and by re-derivation), not hidden. The
> **authoritative, checked** statements are in the paper (`../PAPER.tex`) and the Lean file;
> when the worklog and the paper disagree, the paper wins.

## Contents

- `consolidation_notes.md` — the consolidated analysis (Bourgain–Chang, circularity, the
  function-field model) written up before the final paper pass.
- `PAPER_DEBUTANT.md` — an earlier **French** plain-language write-up of the problem and the
  approach (a candidate source for the English `../EXPLAINER.md`).
- `STEP1-2` … `STEP56` — the full campaign: the sieve reduction, Lemma K, the Type A/B split,
  the almost-all-$n$ theorem, and the salve-by-salve analysis (Stepanov/García–Voloch,
  Bourgain–Chang, circularity, $\mathbb F_q[t]$) with the corrections logged as they happened
  (`STEP45`, `STEP51`, `STEP52`, `STEP54` in particular record caught over-claims).
